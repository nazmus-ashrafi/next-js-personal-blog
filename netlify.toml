[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

# Increase function size limits and optimize bundling
[functions]
  node_bundler = "esbuild"
  external_node_modules = ["sharp", "canvas"]
